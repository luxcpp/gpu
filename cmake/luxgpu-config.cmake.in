# Copyright (c) 2024-2026 Lux Industries Inc.
# SPDX-License-Identifier: BSD-3-Clause-Eco
#
# CMake config for Lux GPU Core
#
# Usage:
#   find_package(luxgpu REQUIRED)
#   target_link_libraries(myapp PRIVATE lux::luxgpu_core)

@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/luxgpu-targets.cmake")

check_required_components(luxgpu)

# Provide convenient alias
if(NOT TARGET lux::gpu)
    add_library(lux::gpu ALIAS lux::luxgpu_core)
endif()
